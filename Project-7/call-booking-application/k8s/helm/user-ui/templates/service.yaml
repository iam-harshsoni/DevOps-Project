apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-ui.fullname" . }}
  labels:
    {{- include "user-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "user-ui.selectorLabels" . | nindent 4 }}
